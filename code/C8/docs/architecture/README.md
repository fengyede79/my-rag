# C8 Architecture Docs

This folder contains the frozen runtime architecture baseline for the C8 recipe RAG system.

Start here:

- `main-runtime-architecture-spec.md`

Future implementation plans, refactors, and tests that touch the main request chain should follow this spec. If a change would alter the main chain, update and review the architecture spec first instead of drifting in code.
